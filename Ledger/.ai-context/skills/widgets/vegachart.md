# Vega Chart

- **Widget ID:** `ledger.widget.web.vegachart.VegaChart`
- **Type:** PLUGGABLEWIDGET
- **Version:** 1.0.0

## MDL Example

```sql
PLUGGABLEWIDGET 'ledger.widget.web.vegachart.VegaChart' widget1
```

## Properties

| Property | Type | Required | Default | Values / notes | Group | Description |
|----------|------|----------|---------|----------------|-------|-------------|
| `spec` | string | Yes |  |  | Chart | Vega-Lite or Vega JSON. Which language is used is decided by the spec's own $schema, so both are accepted here. Any "data" in the spec is replaced by the Data property below when that is set. |
| `chartData` | attribute |  |  |  | Chart | A JSON array of row objects, bound to the spec's named dataset (or to its top-level data when no name is given). Kept separate from the spec so the model never has to generate layout. |
| `datasetName` | string |  | table |  | Chart | The name the spec uses to refer to the data (Vega-Lite: {"data": {"name": "table"}}). Leave empty to replace the spec's top-level data instead. |
| `dataUrl` | attribute |  |  |  | Chart | An address the chart fetches for itself, replacing the spec's top-level data. Bound rather than written into the spec so a filter can reach it: the specification is a static string, so a URL inside it can never carry a query the user chose. Takes precedence over Data — the two are alternatives, and a chart given both would fetch and then discard. Any "format" already on the spec's data is kept, which is where an OData envelope is unwrapped: {"format": {"type": "json", "property": "value"}}. |
| `selection` | attribute |  |  |  | Interaction | Written with the clicked mark's datum as JSON. Only the datum's own scalar fields are included — Vega's internal bookkeeping is stripped, so what arrives is the row the model emitted. |
| `onClick` | action |  |  |  | Interaction | Runs after the selection attribute is written. Leave empty to make the chart read-only; clicks are then ignored entirely rather than being written and discarded. |
| `chartHeight` | integer | Yes | 360 |  | Appearance | Height of the chart container. Zero lets the container take the height the chart renders at, which is what a chart sized by its data needs — a facet row per category or a legend entry per series has no height the page can know in advance. |
| `renderer` | enumeration | Yes | svg | `svg` \| `canvas` | Appearance | SVG keeps marks selectable and styleable by CSS; canvas is faster for very dense charts. |
| `showActions` | boolean | Yes | false |  | Appearance | Vega's own export / view-source menu. Off by default so the chart carries no chrome of its own. |

---

Regenerated by `mxcli widget docs` and by `refresh catalog`. For the same data live from the `.mpk` — including anything added by a widget upgrade since this file was written — run `mxcli widget describe vegachart -p <app.mpr>`.
